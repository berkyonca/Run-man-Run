using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class camFollow : MonoBehaviour
{ /*
    playerMove playerErisim = new playerMove();
    public Transform targetPos;
    public bool spaceBasildi = false;
    [SerializeField] Vector3 offSet;
    private void FixedUpdate()
    {
        if (!Input.GetKey(KeyCode.Space))
        {
            Vector3 nextPos = targetPos.position + offSet;
            transform.position = Vector3.Lerp(transform.position, nextPos, Time.deltaTime * 6f);
        }
        if (Input.GetKeyDown(KeyCode.Space))
        {
            spaceBasildi = !spaceBasildi;
        }
        StartCoroutine(CamGlitch());
    }

    IEnumerator CamGlitch()
    {
        yield return new WaitForSeconds(.1f);
        if (Input.GetKey(KeyCode.Space))
        {
            transform.Translate(Vector3.left * Time.deltaTime * 200f);
        }
    }

  */
}
